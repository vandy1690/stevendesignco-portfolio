---
import Site from '../../layouts/Site.astro';
import PayPalHero from '../../components/animated/PayPalHero';
---

<Site
	title="PayPal: Becoming the Default at Checkout · Steven Vanden Heuvel"
	description="Merchant checkout proposals for the one-click program, 20+ of them on weekly sprints for top global merchants, and the consumer-side checkout dead end mapped for senior leadership."
	image="/og/paypal.png"
>
	<section class="cs-hero">
		<div class="cs-hero__inner">
			<a class="cs-back-top" href="/#work">← Case studies</a>
			<p class="eyebrow">Case study</p>
			<h1 class="cs-hero__title display">PayPal: Becoming the Default at Checkout</h1>
			<p class="cs-hero__lede">PayPal is competing with Apple Pay and Google Pay to be the default payment method at mobile checkout. The way to win it is one-click checkout: save a customer's payment details securely so buying takes one tap instead of filling a form. My work was showing enterprise merchants what that looks like inside their own checkout, in their own brand, mostly on mobile.</p>
			<dl class="cs-meta">
				<div><dt>Role</dt><dd>Principal Designer · PayPal</dd></div>
				<div><dt>Team</dt><dd>Merchant Enablement</dd></div>
			</dl>
			<p class="cs-note">This work is proprietary to PayPal. Flows are shown generically, so they are not reproduced here.</p>
		</div>
	</section>

	<section class="section section--alt">
		<div class="section__inner cs-orbit" data-animate="fade">
			<p class="eyebrow">The stage</p>
			<h2 class="cs-h2 display">PayPal partners with the top 100 merchants. The job was the checkout in front of them.</h2>
			<div class="cs-orbit__art">
				<PayPalHero client:load />
			</div>
		</div>
	</section>

	<section class="section">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">What was unclear</p>
				<h2 class="cs-block__lede">PayPal showed up differently at every checkout.</h2>
				<p>Each merchant integration carried its own patterns. Across vaulting, express, and standard flows, the experience changed from one surface to the next: app switch versus web, first time versus returning. That inconsistency worked against the one-click program, where the whole point is that paying takes one tap in any merchant's checkout.</p>
			</div>
		</div>
	</section>

	<section class="section section--alt">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">What I clarified</p>
				<h2 class="cs-block__lede">One proposal per merchant, on a weekly sprint.</h2>
				<p>On the Merchant Enablement team I produced a proposal package for each merchant. Each one mapped the current checkout against a clearer target state across the four entry contexts, Wallet, Express, Standard first time, and Standard return, and the major variants: app switch versus web, Pay Later versus Pay in Full, passkey, and vaulting.</p>
				<p>Every package was the same stack: a screen audit, the complete checkout flows, a working prototype, a prototype video, and the decks for the merchant conversation.</p>
			</div>
		</div>
	</section>

	<section class="section">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">The constraint</p>
				<h2 class="cs-block__lede">Three design libraries were live at once, and checkout was locked to the oldest.</h2>
				<p>Versions 4.0, 5.1 and 5.2, and a 6.0 target ran side by side, with Oslo incoming, and checkout stayed on the oldest because engineering could not move. Part of the job was telling product managers which blue-sky work was presentable now and which was years from shipping. Every proposal came out of that constrained library.</p>
				<p>A second thread ran alongside the top accounts: a modular checkout flow library for merchants outside the top 100, covering grocery, retail, subscriptions, and restaurants, mostly mobile, from product selection through payment confirmation. Those generic flows are the ones that can be shown.</p>
			</div>
		</div>
	</section>

	<section class="section section--alt">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">Who I influenced</p>
				<h2 class="cs-block__lede">The package was what the team took into the room.</h2>
				<p>PMs and design leads used it in merchant conversations. I worked from PM briefs, with Principal Designer review and peer review inside a senior team. The proposals fed the merchant pitch for brands including Home Depot, Nintendo, Target, Best Buy, TikTok Shop, Chewy, and Microsoft.</p>
			</div>
		</div>
	</section>

	<section id="checkout-dead-end" class="section section--alt">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">The consumer side</p>
				<h2 class="cs-block__lede">The checkout dead end</h2>
				<p>A customer reaches checkout and finds there is no card that can pay. The card expired, or it was declined, or it was removed, or it was never added in the first place. The order stops there.</p>
				<p>I built the Figma board that mapped that dead end back to every earlier moment the customer could have fixed the card. The board traced the touchpoints across roughly a month before checkout, marked which ones send a message today and which ones send nothing, and ended on the checkout experience itself.</p>
				<p>The structure came together fast because I used Claude Code to turn a Design Director's diagram into the underlying framework of the board, then designed into it. The screens came from teams across the company, which meant tracking down owners in parts of the organization I had not worked with before. The work was presented to senior leadership, and the board became the template the team now uses to map the next flows.</p>
				<p class="cs-caption">PayPal, 2026. Figma, Claude Code, cross-team research.</p>
			</div>
		</div>
	</section>

	<section class="section">
		<div class="section__inner cs-body" data-animate="fade">
			<div class="cs-block">
				<p class="eyebrow">What changed</p>
				<h2 class="cs-block__lede">Twenty-plus packages in my first six months, and the tooling to hold the pace.</h2>
				<p>The product manager on that work credited the team's merchant-specific proposals and demos with moving several top accounts to contract, integration, or launch on new PayPal checkout enhancements. Wayfair selected PayPal to test upstream presentment on product and cart pages for a new guest checkout. I never saw conversion data. It was proposal work.</p>
				<p>Holding a weekly cadence at that volume is its own design problem. The tooling was not part of my assignment, and I built it because the handoff into each package was the part that kept breaking. That story is its own case study: <a href="/work/merchant-flow-builder">Merchant Flow Builder</a>.</p>
			</div>
			<ul class="cs-stats">
				<li><strong>20+</strong><span>Proposal packages shipped</span></li>
				<li><strong>15 min</strong><span>Per merchant, from most of a day</span></li>
			</ul>
		</div>
	</section>

	<section class="section section--alt">
		<div class="section__inner cs-back">
			<a class="pager pager--prev" href="/#work" aria-label="Back to all case studies"><span class="pager__cap" aria-hidden="true"><svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 3.5 5.5 8l4.5 4.5"/></svg></span>All case studies</a>
			<a class="pager" href="/work/plate" aria-label="Next case study: Plate">Plate<span class="pager__cap" aria-hidden="true"><svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m6 3.5 4.5 4.5L6 12.5"/></svg></span></a>
		</div>
	</section>
</Site>

<style>
	.cs-hero { max-width: var(--max); margin: 0 auto; padding: var(--space-2xl) var(--gutter); }
	.cs-back-top { display: inline-flex; align-items: center; min-height: 24px; margin-bottom: var(--space-lg); color: var(--text-mute); font-size: var(--text-body-sm); }
	.cs-back-top:hover { color: var(--accent); }
	.cs-hero__title { margin: var(--space-xs) 0 var(--space-lg); max-width: 20ch; }
	.cs-hero__lede { font-size: var(--text-deck); color: var(--text); line-height: 1.5; max-width: 56ch; margin-bottom: var(--space-2xl); font-weight: 500; }
	.cs-meta { display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-md); margin: 0; padding: var(--space-lg) 0 0; border-top: 1px solid var(--rule); }
	.cs-meta div { margin: 0; }
	.cs-meta dt { font-size: var(--text-label); letter-spacing: 0.14em; text-transform: uppercase; color: var(--text-mute); font-weight: 800; margin-bottom: var(--space-xs); }
	.cs-meta dd { margin: 0; color: var(--text); font-size: var(--text-body); font-weight: 500; }
	.cs-note { margin: var(--space-md) 0 0; padding: 0; border: 0; font-family: var(--font-body); font-weight: 400; font-size: 14px; line-height: 1.6; letter-spacing: 0.04em; color: var(--text-mute); max-width: 64ch; }

	.cs-orbit { display: grid; gap: var(--space-md); }
	.cs-orbit__art { margin-top: var(--space-md); }
	.cs-h2 { max-width: 22ch; margin: 0 0 var(--space-sm); }

	/* One organism grouping: label, subhead and body sit tight together, and the
	   space between groupings is 25 percent narrower than it was. */
	.section { padding-top: var(--space-xl); padding-bottom: var(--space-xl); }

	.cs-body { display: grid; gap: var(--space-xl); max-width: var(--max-narrow); }
	/* Hierarchy: eyebrow + subhead group as the headline, body is support. */
	.cs-block .eyebrow { margin-bottom: 0; }
	/* case-study eyebrows: nickel, normal weight, .03em, black */
	.eyebrow { font-family: var(--font-display); font-weight: 400; font-variation-settings: "wght" 400, "slnt" 0, "ital" 0; font-synthesis: none; letter-spacing: 0.03em; }
	.cs-block__lede { color: var(--text); font-size: clamp(24px, 2.5vw, 32px); font-weight: 600; margin: 0; letter-spacing: -0.015em; line-height: 1.25; }
	.cs-block p { color: var(--text-mute); margin: 0; font-size: 16px; line-height: 1.65; }
	.cs-block p + p { margin-top: 0; }
	.cs-block p:not(.eyebrow):not(.cs-block__lede) + p:not(.eyebrow):not(.cs-block__lede) { margin-top: var(--space-sm); }
	.cs-block .cs-caption { margin-top: var(--space-md); color: var(--text-mute); font-size: 14px; letter-spacing: 0.04em; }

	.cs-stats { list-style: none; margin: 0; padding: 0; display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-md); }
	.cs-stats li { padding: var(--space-lg) 0; border-top: 1px solid var(--rule); }
	.cs-stats strong { display: block; font-family: var(--font-display); font-weight: 900; font-style: normal; font-variation-settings: "wght" 900, "slnt" 0, "ital" 0; font-synthesis: none; font-size: clamp(48px, 6vw, 72px); color: var(--text); line-height: 1; letter-spacing: -0.02em; margin-bottom: var(--space-xs); }
	.cs-stats span { color: var(--text-mute); font-size: 14px; letter-spacing: 0.06em; text-transform: uppercase; }

	.cs-back { display: flex; gap: var(--space-sm); flex-wrap: wrap; justify-content: space-between; align-items: center; }
	.cs-back .btn--primary { background: var(--text); color: var(--bg); border-color: var(--text); }
	.cs-back .btn--primary:hover { background: var(--text); border-color: var(--text); color: var(--bg); box-shadow: none; }

	@media (max-width: 880px) {
		.cs-orbit__art {
			width: 100%;
			aspect-ratio: 1 / 1;
			overflow: visible;
			margin-top: var(--space-md);
		}
		.cs-hero__title { font-size: clamp(32px, 9vw, 56px); }
	}

	@media (max-width: 700px) {
		/* Pagers stay side by side on phones: they share the row and wrap their
		   own label rather than dropping to two stacked buttons. */
		.cs-back { flex-wrap: nowrap; gap: 10px; align-items: stretch; }
		.cs-back .pager { flex: 1 1 0; min-width: 0; height: auto; min-height: 44px; padding: 9px 40px 9px 12px; font-size: 13px; line-height: 1.25; white-space: normal; }
		.cs-back .pager--prev { padding: 9px 12px 9px 40px; }
		.cs-back .pager__cap { width: 32px; }
		.cs-meta { grid-template-columns: 1fr; gap: var(--space-lg); }
		.cs-meta dt { font-size: 13px; letter-spacing: 0.1em; color: var(--text); margin-bottom: 2px; }
		.cs-meta dd { font-size: 17px; }
		.cs-stats { grid-template-columns: 1fr; }
	}

	/* Case study links read as text, not pills: underline, accent on hover. */
	.cs-artifacts .btn, .cs-back .btn, .cs-back .btn--primary {
		padding: 0;
		border: 0;
		border-radius: 0;
		background: transparent;
		color: var(--text);
		font-family: var(--font-body);
		font-weight: 600;
		font-size: 15px;
		letter-spacing: 0;
		text-decoration: underline;
		text-decoration-thickness: 1px;
		text-underline-offset: 5px;
		transform: none;
		box-shadow: none;
	}
	.cs-artifacts .btn:hover, .cs-back .btn:hover, .cs-back .btn--primary:hover {
		color: var(--accent);
		background: transparent;
		border: 0;
		transform: none;
		box-shadow: none;
	}
</style>
